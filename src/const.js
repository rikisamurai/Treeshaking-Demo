/*＃NO_SIDE EFFECTS*/
const addName /*#__NO_SIDE_EFFECTS__*/ = i => i + 'name';

const name = /*#__PURE__*/ addName('aakncaskjdnkjs1');
export const Protocol = location.host.includes('boe') ? 'http' : 'https';
export const Protocol1 = location.host.includes('boe') ?? 'https';
console.log(function bar(price) {
  return price || 0;
});
console.log(function bar(price) {
  return price ?? 0;
});

export const URL = {
  home: `${Protocol}https://home`,
  blog: 'https://home/blog',
  settings: 'https://home/settings',
  profile: 'https://home/profile',
};

export const kiki = /*#__PURE__*/ addName('kiki');
export const riki = addName('riki');
export const mugen = addName('mugen');

export function foo(price) {
  return price ?? 0;
}

export function bar(price) {
  return price || 0;
}
